# موقع الجزري — الخطوة ١: الأساس

هذا الملف يشرح شنو انبنى وشلون تشغّلينه.

---

## ١. التشغيل

بمجلد فاضي شغّلي:

```bash
npx create-next-app@latest aljazari --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-turbopack
cd aljazari
npm install next-intl
```

بعدين انسخي ملفات هذا المجلد فوق المشروع (تستبدل الموجود)، وشغّلي:

```bash
npm run dev
```

افتحي `http://localhost:3000` — راح يحوّلك تلقائياً لـ `/ar`.
اضغطي زر **English** فوق: يوديچ لـ `/en` بنفس الصفحة، والاتجاه ينقلب لـ LTR لوحده.

**علامات النجاح:** الخلفية بنفسجية متدرجة، الخط عربي حديث، تظهر "5 robots loaded"، وبطاقات Pepper وTimo وCRUZR وBellaBot Pro وNAO.

---

## ٢. شنو موجود بالمشروع

```
src/
├── i18n/
│   ├── routing.ts        ← اللغات والافتراضية (ar) وشكل الروابط
│   ├── navigation.ts     ← Link و useRouter بدل مالة Next العادية
│   └── request.ts        ← تحميل القاموس حسب اللغة
├── middleware.ts         ← يحوّل / إلى /ar تلقائياً
├── lib/types.ts          ← أنواع البيانات (LocalizedText وغيرها)
├── data/
│   ├── robots.ts         ← ★ 15 روبوت كامل باللغتين
│   ├── taxonomy.ts       ← التصنيفات والقطاعات
│   └── company.ts        ← المهمة، الرؤية، الخدمات، الضمان، العملاء، الشركاء
├── components/
│   └── LocaleSwitcher.tsx
└── app/
    ├── globals.css       ← ألوان الهوية والخطوط
    └── [locale]/
        ├── layout.tsx    ← RTL/LTR + الخطوط + metadata + hreflang
        └── page.tsx      ← مؤقتة، تنستبدل باچر

messages/
├── ar.json
└── en.json
```

---

## ٣. القاعدة الذهبية باللغتين

**فصل واضح:**

| النوع | وين ينحط | مثال |
|---|---|---|
| نصوص الواجهة (أزرار، عناوين أقسام، رسائل) | `messages/*.json` | "احجز روبوتك" / "Book a robot" |
| محتوى الشركة (روبوتات، خدمات) | `data/*.ts` بحقل `{ ar, en }` | وصف Pepper |
| مصطلحات تقنية | تبقى لاتينية بالنسختين | LiDAR, SLAM, ROS 2, IMU |
| أسماء الروبوتات | لاتينية بالنسختين، ما تُعرّب | Pepper مو "بيبر" |
| القيم الرقمية | مرة وحدة بحقل `value` | `19 KG`, `13.3 inch` |

**ما أستخدمت ترجمة آلية بأي سطر** — كل النصوص الإنجليزية مكتوبة يدوياً، لأن الترجمة الآلية بالضبط هي الي تكسّر المصطلحات التقنية.

**للـ RTL:** استخدمي دائماً الخصائص المنطقية بدل اليمين/اليسار:

```
✅  ps-4  pe-4  ms-2  me-2  text-start  text-end  start-0  end-0
❌  pl-4  pr-4  ml-2  mr-2  text-left   text-right left-0  right-0
```

هيچ التصميم ينقلب صح باللغتين بدون ما تكتبين CSS مرتين.

---

## ٤. الهوية البصرية

الألوان مأخوذة من الكتالوغ المطبوع مو من قالب جاهز:

| التوكن | اللون | الاستخدام |
|---|---|---|
| `brand-950` | `#170a2e` | أغمق نقطة بالتدرّج |
| `brand-800` | `#43197a` | وسط التدرّج |
| `brand-700` | `#5e2ca5` | البنفسجي الأساسي |
| `brand-400` | `#a472ec` | التفاعل والـ hover |
| `brand-300` | `#c0a0f5` | الأسطر الثانوية والتفاصيل |
| `paper` | `#f6f3fb` | خلفية الأقسام الفاتحة |

استخدميها بتيلويند مباشرة: `bg-brand-800`، `text-brand-300`، وللتدرّج الكامل `bg-brand-gradient`.

**الخطوط:** `Readex Pro` يمسك العربي والإنجليزي بصوت واحد (مهم حتى الموقع ما يصير موقعين مختلفين عند التبديل)، و`IBM Plex Mono` **بس** للأرقام والمواصفات وتسميات الحساسات — لأن هذا صوت الكتالوغ التقني.

**التوقيع البصري** الي راح نبني عليه صفحة كل روبوت: الخطوط المنقّطة الي تؤشّر على أجزاء الروبوت — نفس شكل الكتالوغ بالضبط. عشان هذا حقل `hardware` بكل روبوت بيه `side: 'start' | 'end'` يحدد جهة الخط، وينقلب لوحده بين العربي والإنجليزي. الـ utility جاهزة بـ `globals.css` باسم `leader-line`.

---

## ٥. إضافة روبوت جديد

افتحي `src/data/robots.ts`، انسخي أي عنصر، وبدّلي محتواه. الموقع كله يلتقطه تلقائياً: البطاقة، صفحة التفاصيل، الـ sitemap، والـ metadata. ما تحتاجين تلمسين أي صفحة.

---

## ٦. ناقص من الشركة (لا يوقف الشغل)

هاي أشياء ما موجودة بالكتالوغ ولازم نطلبها — حطيتها placeholders حتى ما تعرقلنا:

1. **الصور** — كل مسارات `/robots/*.png` فاضية. نحتاج صور الروبوتات بخلفية شفافة. مؤقتاً نكدر نقص الصور من الكتالوغ نفسه.
2. **الشعارات** — شعارات العملاء والشركاء (`/clients/`, `/partners/`).
3. **معلومات التواصل** — الهاتف، واتساب، الإيميل، العنوان، روابط السوشيال (`CONTACT` بملف `company.ts`).
4. **الشركة المصنّعة لـ Wnno** — الكتالوغ ما ذكرها، خليت الحقل فاضي.
5. **مواصفات Unitree** — ظهر بغلاف الكتالوغ بس بدون صفحة تفاصيل، فمؤشّر `draft: true` ولا يظهر بالقوائم لحد ما تجي معلوماته.
6. **Ugot و Ukit** — الكتالوغ ذكر مميزاتهم بدون مواصفات رقمية، فـ `specs` فاضية عندهم (الصفحة تتعامل معها طبيعي).

---

## ٧. أخطاء الكتالوغ الي صُحّحت بالموقع

- `UITRASONIC` → **ULTRASONIC**
- `aetuators` → **actuators**
- "الجـلاي" بصفحة من نحن → **الجزري**

---

## الخطوة الجاية (باچر)

الصفحة الرئيسية الحقيقية + الـ 3D hero + برومبتات React Bits + صفحات الروبوتات.
